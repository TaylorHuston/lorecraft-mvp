---
kind: sdd-epic-verify-report
epic: LC-002
epic_path: docs/epics/lc-002-world-adventure-model/epic.md
created: 2026-07-07
result: changes-requested
mode: default
---

# Epic Verify: LC-002 World / Adventure Model

## Verdict

- Result: `changes-requested`
- App root: `/Users/taylor/src/my-life/my-vault/03-spaces/spaces-code/lorecraft-mvp`
- Epic: `docs/epics/lc-002-world-adventure-model/epic.md`
- Branch/ref: `develop` at `4e03c7b`
- Delegation: bounded SDD subagent permission used for Epic coherence and implementation/evidence drift audits
- Report mode: default, report written; no Epic or source files changed by this verification

LC-002 is the right Epic boundary for World/Adventure. It should not be merged into LC-001. The current shape is coherent: Worlds and WorldVersions are seed/source material, Adventures are frozen playable instances, and Adventure-scoped runtime state isolates player sessions. S1-S4 should stay together because create, resume, delete, reset, and runtime isolation are one user path.

Do not split the Epic yet. The only split candidate is the Tutorial seed world if it grows into onboarding curriculum, scenario design, or a content-authoring system. For now it can remain as a seeded-world acceptance exercise inside LC-002.

This Epic does need follow-up before it can be treated as fully aligned. The artifact lifecycle is stale (`status: draft` despite implemented Stories), and the implementation appears Stormbound-specific in some debug reset/accounting paths, which can break Tutorial or future seed worlds.

## Gate Scorecard

| Gate | Result | Notes |
|---|---|---|
| SDD doctrine adherence | findings | Epic is capability-oriented and maps to a clear user path. |
| Epic coherence | pass | World/Adventure is separate from the play loop and should remain separate. |
| Epic template adherence | findings | Template checker reports missing top-level `Notes` and missing `Story Notes` for S1-S4. |
| Story shape | pass | S1-S4 form the core model; S5 is acceptable as a seeded-world acceptance story for now. |
| Story requirement completeness | findings | Direct URL missing/deleted handling, Reset World semantics, and multi-world debug reset behavior need clearer ownership. |
| Story reference traceability | findings | Evidence is plausible but broad; S3/S4 need focused automated tests. |
| Requirement and Scenario truth | findings | Adventure isolation and reset design align; Tutorial/debug reset paths need implementation follow-up. |
| Implementation drift | changes requested | Debug NPC reset and debug-created entity accounting appear tied to Stormbound seed constants. |
| Verification strength | findings | CI passed, but E2E was not rerun and Convex-level isolation tests are thin. |
| Change lifecycle traceability | findings | Closed change Resume Here still describes in-progress closeout. |
| Docs and product alignment | findings | ADR is aligned but related Epic links are incomplete. |
| Security and data safety | deferred | Destructive debug mutations remain local prototype scope. |

## Verification Matrix

| Story | Requirement | Scenario | Current Evidence | Evidence Type | Check Run | Result | Gap |
|---|---|---|---|---|---|---|---|
| LC-002-S1 | R1 | R1-S1 Start a new Adventure from a visible World | Adventure creation copies selected baseline rows and records source IDs | source inspection, prior E2E evidence | `npm run ci:required` | aligned | Add focused copy-content assertion. |
| LC-002-S1 | R1 | R1-S2 Resume an existing Adventure | Home lists Adventures under World containers; direct URLs load Adventure state | source inspection, prior E2E evidence | `npm run ci:required` | aligned | Direct missing/deleted URL behavior needs explicit scenario ownership. |
| LC-002-S1 | R1 | R1-S3 Delete an Adventure | Delete mutation removes Adventure-owned rows | source inspection, prior E2E evidence | `npm run ci:required` | aligned | Add focused cascade assertion. |
| LC-002-S2 | R1 | R1-S1 Adventure-scoped feed/runtime reads | read model loads commands, narrations, utility, events, and state by `adventureId` | focused tests, source inspection | `npm run ci:required` | aligned | None blocking. |
| LC-002-S2 | R1 | R1-S2 Adventure-scoped writes | turn, utility, debug, NPC, and location mutations are scoped to Adventure rows | focused tests, source inspection | `npm run ci:required` | aligned | Broaden tests for multiple Adventures. |
| LC-002-S2 | R1 | R1-S3 Prompt/debug context shows source version | Director context includes Adventure/source identifiers | source inspection | `npm run ci:required` | aligned | Evidence is broad. |
| LC-002-S3 | R1 | R1-S1 WorldVersion edits do not mutate existing Adventures | New versions are created; existing Adventures retain stored source version | source inspection | `npm run ci:required` | needs verification | Add deterministic Convex-level isolation test. |
| LC-002-S3 | R1 | R1-S2 Future Adventures use selected/current WorldVersion | creation from current WorldVersion exists | source inspection | `npm run ci:required` | aligned | Focused test recommended. |
| LC-002-S4 | R1 | R1-S1 Reset Session restores Adventure from its source WorldVersion | reset deletes selected runtime rows and recopies source baseline | source inspection, prior E2E evidence | `npm run ci:required` | aligned | Add v2 source-version reset test. |
| LC-002-S4 | R1 | R1-S2 Reset does not mutate source WorldVersion or other Adventures | source version remains immutable by design | source inspection | `npm run ci:required` | needs verification | Add focused multi-Adventure reset assertion. |
| LC-002-S5 | R1 | R1-S1 Tutorial world can create an Adventure | Tutorial seed content and selected-World creation exist | source inspection, prior E2E evidence | `npm run ci:required` | aligned | E2E not rerun in this audit. |
| LC-002-S5 | R1 | R1-S2 Tutorial introduces multiple NPC/location test surface | Tutorial content contains later room/NPC coverage | source inspection | `npm run ci:required` | partially verified | E2E does not prove reaching multi-NPC room or `/look` there. |

## Findings

### BLOCKING

- None.

### REQUIRED

1. `implementation-drift` - Tutorial and future seed worlds can be mishandled by Stormbound-specific debug reset/accounting paths.
   - Evidence: delegated implementation audit found `resetNpcInternal` restores from Stormbound `SEEDED_NPCS` and deletes unknown NPCs, and debug-created NPC/location counters use Stormbound seed constants.
   - Impact: resetting Guide Serin, Mara, Orin, or future seed-world NPCs individually can delete or misclassify baseline entities instead of restoring the Adventure's source WorldVersion.
   - Recommended workflow: small SDD fix. Make debug reset/accounting source-version aware, or explicitly limit per-entity debug reset to Stormbound until generalized.

2. `lifecycle-drift` - LC-002 status is stale.
   - Evidence: frontmatter still says `status: draft` even though S1-S5 are implemented and this Epic has been through apply/review/merge.
   - Impact: status no longer reflects the source of truth.
   - Recommended workflow: artifact-only status and `last_verified` update after follow-up decision.

3. `template-drift` - LC-002 is missing required Epic/Story sections.
   - Evidence: template checker reported missing top-level `Notes`, plus missing `Story Notes` for S1-S4.
   - Impact: the Epic does not meet the current SDD template.
   - Recommended workflow: artifact-only fix.

4. `verification-drift` - S3/S4 rely too much on source inspection and live smoke evidence.
   - Evidence: WorldVersion isolation and reset-to-source behavior are central to LC-002 but lack focused deterministic tests for multi-Adventure and edited source-version cases.
   - Impact: frozen-copy semantics could regress without quick feedback.
   - Recommended workflow: add focused Convex-level tests or route-level integration tests.

5. `lifecycle-drift` - closed change closeout text is stale.
   - Evidence: `docs/changes/closed/2026-07-01-world-adventure-model/tasks.md` still describes a Resume Here state in the middle of closeout even though the folder is closed and merged.
   - Impact: future readers can mistake closed work for active work.
   - Recommended workflow: artifact-only historical closeout cleanup.

### SUGGESTION

1. `scope-clarity` - Add explicit ownership for direct `/adventures/<id>` missing/deleted/malformed behavior.
   - This is part of the Adventure URL user path and belongs in LC-002.

2. `scope-clarity` - Add a small requirement distinguishing Reset Session from Reset World.
   - Reset Session is Adventure-scoped. Reset World reseeds local demo source material and should not be confused with Adventure reset.

3. `future-split-trigger` - Keep Tutorial in LC-002 for now.
   - Split it only if Tutorial becomes onboarding curriculum, authored content, or playtest scenario design rather than a seeded-world acceptance fixture.

4. `docs-traceability` - Update ADR related links.
   - The frozen-copy ADR should reference LC-002 directly, not only LC-001.

## Split / Merge / Refactor Assessment

- Split now: no.
- Merge with LC-001: no. LC-002 is the container/versioning model; LC-001 is the play loop.
- Refactor now: yes, light artifact refactor plus a small implementation follow-up.
- Suggested future split triggers:
  - Tutorial grows into an onboarding or authored scenario Epic.
  - World Builder creates or edits WorldVersions through a user-facing UI.
  - Adventure branching, rollback, sharing, or ownership becomes a separate user path.

## Drift Summary

- Doctrine drift: low; Epic is a good user-path container.
- Template drift: missing `Notes` and S1-S4 `Story Notes`.
- Artifact drift: status still draft; closed change Resume Here stale; ADR related links incomplete.
- Implementation drift: Stormbound-specific debug reset/accounting behavior threatens multi-world correctness.
- Requirement drift: direct Adventure URL handling and Reset World semantics need clearer ownership.
- Verification drift: S3/S4 need focused deterministic isolation/reset tests; Tutorial multi-NPC path is only partially verified.
- Scope drift: Tutorial is still acceptable in LC-002 but should split if it becomes real onboarding content.
- Product drift: none blocking.
- Lifecycle drift: Epic status and closed change closeout are stale.
- Superseded-truth drift: minor ADR link gap.
- Security drift: debug destructive mutations remain accepted local-prototype scope.

## Tests And Checks

| Command / Scenario | Result | Proves | Notes |
|---|---|---|---|
| `scripts/epic_template_check.py docs/epics/lc-001-provider-agnostic-chat-experience/epic.md docs/epics/lc-002-world-adventure-model/epic.md --format markdown` | fail | Canonical Epic section spine and Story metadata | LC-002 missing top-level `Notes` and S1-S4 `Story Notes`. |
| `npm run ci:required` | pass | ESLint, Vitest, typecheck, Next production build | 7 test files, 80 tests passed; Next 16.2.9 build passed. |
| `npx convex codegen` | pass | Convex function/schema validation and generated bindings | App git status remained clean after codegen. |
| `npm run e2e` | not run | Would prove full browser flow | Not run because local dev server and Convex backend must remain running; project docs say E2E owns those ports and local test state. |

## Delegated Audits

- Epic coherence: recommended keeping LC-002 separate and not splitting S1-S4; Tutorial can remain until it becomes onboarding/content design.
- Implementation/evidence drift: found implementation mostly aligned, but identified Stormbound-specific per-entity debug reset/accounting and under-tested WorldVersion isolation/reset paths.
- Claims verified by main agent: template checker, CI, Convex codegen, current docs, current schema/routes/tests, and app git status.

## Remediation Plan

| Finding | Recommended Workflow | Owner Decision Needed |
|---|---|---|
| Stormbound-specific debug reset/accounting | `sdd-propose -> sdd-apply -> sdd-review` or small follow-up under existing Epic | Decide whether to generalize now or explicitly limit debug reset scope. |
| LC-002 status and template sections | artifact-only | None after deciding implementation follow-up timing. |
| S3/S4 deterministic isolation/reset tests | SDD test-hardening change | Decide whether to bundle with debug reset fix. |
| Closed change Resume Here cleanup | artifact-only | None. |
| Direct Adventure URL and Reset World ownership | artifact-only | None unless behavior changes. |

## Evidence Inspected

- Epic: `docs/epics/lc-002-world-adventure-model/epic.md`
- Change artifacts: `docs/changes/closed/2026-07-01-world-adventure-model/`
- Code: `convex/schema.ts`, `convex/world.ts`, `src/lib/world/convex-snapshot-read-model.ts`, `src/lib/world/convex-director-context.ts`, `src/lib/world/convex-turn-persistence.ts`, `src/lib/world/stormbound-baseline.ts`
- Tests: `src/app/api/director/turn/route.test.ts`, `src/lib/world/convex-snapshot-read-model.test.ts`, `tests/e2e/lorecraft-playtest.spec.ts`
- Docs: `README.md`, `docs/data-model.md`, `docs/persistence-system.md`, `docs/adrs/2026-07-01-world-adventure-frozen-copies.md`, `docs/testing.md`

## Residual Risks

- E2E was not rerun in this audit to preserve the running dev server.
- Frozen-copy semantics are central and should have focused automated tests before the app depends on multiple authored Worlds.
- Tutorial behavior is currently a seed fixture; it has not yet been manually accepted as an onboarding experience.

## Next Action

- Create a small follow-up to make debug reset/accounting source-version aware and add focused deterministic tests for WorldVersion isolation and Adventure reset.
- In the same pass or an artifact-only pass, update LC-002 status, template sections, ADR links, closed change closeout wording, and direct Adventure URL/Reset World ownership notes.
